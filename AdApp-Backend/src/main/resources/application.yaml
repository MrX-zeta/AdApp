ktor:
    application:
        modules:
            - com.adapp.backend.ApplicationKt.module
    deployment:
        port: 8081
        # Configuración para soportar archivos grandes (audio)
        maxRequestSize: 52428800  # 50 MB en bytes
jwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
postgres:
  url: "jdbc:postgresql://localhost:5432/Adapp"
  driver: "org.postgresql.Driver"
  user: "postgres"
  password: "12345678"
  maxPoolSize: 10
